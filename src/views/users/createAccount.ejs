<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/head') %>
    <!--<link rel="stylesheet" href="./css/styleregistro.css" />-->
    <title>Login Form</title>
  </head>
  <body class="register">
    <%- include('../partials/header') %>

    <div class="main-container-registro">
      <form
        action="/createaccount"
        method="POST"
        class="contenedor-formulario"
        enctype="multipart/form-data"
      >
        <div class="container-registro">
          <h1>Crea tu nueva cuenta</h1>
          <p>Por favor llena estos datos para acceder</p>
          <p>Este registro no distingue entre mayusculas o minusculas</p>

          <label for="name">Nombre(s)</label>
          <input
            type="text"
            name="name"
            id="name"
            placeholder="Ingresa tu nombre"
            oninput= "this.value = this.value.toUpperCase()"
            required
          />
          <label for="lastname">Apellidos</label>
          <input
            type="text"
            name="lastname"
            id="lastname"
            placeholder="Ingresa tus apellidos"
            oninput= "this.value = this.value.toUpperCase()"
            required
          />
          <label for="username">Nombre de Usuario</label>
          <input
            type="text"
            name="username"
            id="username"
            placeholder="Ingresa Nombre de Usuario"
            oninput= "this.value = this.value.toUpperCase()"
            required
          />
          <label for="email">E-mail</label>
          <input
            type="email"
            name="email"
            id="email"
            placeholder="Ingresa tu email"
            oninput= "this.value = this.value.toUpperCase()"
            required
          />

          <label for="password">Contraseña</label>
          <input
            type="password"
            placeholder="Ingresa la Contraseña"
            name="password"
            id="password"
            oninput= "this.value = this.value.toUpperCase()"
            required
          />
          <label for="cofpassword"
            >Confirmación de Contraseña 
          </label>
          <!--Esto debe cambiarse para que ayude al usuario a no errar al establecer contraseña-->
          <input 
            type="password"
            placeholder="Confirma tu contraseña"
            id="cofpassword"
            oninput= "this.value = this.value.toUpperCase()"
            required
          />
          <label for="img">Imagen de usuario</label>
          <input type="file" name="img" id="img" required />

          <div class="container-buttons">
            <button type="submit" class="btn">Accede</button>
            <button onclick="openLoginForm()" class="btn-existingAccount">
              Aqui si ya tienes cuenta
            </button>
          </div>
        </div>
      </form>
    </div>
    <%- include('../partials/footer') %>
  </body>

  <script src="js/home.js"></script>
</html>
